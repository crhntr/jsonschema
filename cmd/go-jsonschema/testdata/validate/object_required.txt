go-jsonschema validate --schema-file schema.json instance.json

-- schema.json --
{
  "type": "object",
  "required": ["name", "age"],
  "properties": {
    "name": { "type": "string" },
    "age":  { "type": "integer", "minimum": 0 }
  }
}
-- instance.json --
{"name": "Ada", "age": 37}
