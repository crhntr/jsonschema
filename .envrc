export GOEXPERIMENT=jsonv2
# Go 1.27 renamed the jsonv2 `inline` struct-tag option to `embed` and
# silently ignores unknown options, which breaks SchemaObject.Extra.
# Pin the toolchain until the repo migrates off the Go 1.26 experiment.
export GOTOOLCHAIN=go1.26.5